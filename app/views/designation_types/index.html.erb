<h1>Listing designation_types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @designation_types.each do |designation_type| %>
      <tr>
        <td><%= designation_type.name %></td>
        <td><%= link_to 'Show', designation_type %></td>
        <td><%= link_to 'Edit', edit_designation_type_path(designation_type) %></td>
        <td><%= link_to 'Destroy', designation_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Designation type', new_designation_type_path %>
